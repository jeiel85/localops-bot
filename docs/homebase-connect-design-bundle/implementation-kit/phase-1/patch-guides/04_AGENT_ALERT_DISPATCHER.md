# Patch 04 — AlertDispatcher

Replace:

```text
src/LocalOpsBot.Agent/Services/AlertDispatcher.cs
```

with:

```text
replacement-files/src/LocalOpsBot.Agent/Services/AlertDispatcher.cs
```

The replacement preserves policy and persistence while removing the direct Telegram dependency.
